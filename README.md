# java-filmorate
Template repository for Filmorate project.

Добавил диаграмму по ссылке: src\main\resources\diagramma_filmorate.jpg
Пояснения: 
- выделил отдельный столбец для типов переменных;
- т.к. у каждого фильма может быть много жанров, вывел в отдельную таблицу
- в столбе, указывающего тип переменной, отдельно выделил требования для строки
- частично начал делать следующий спринт, поэтому в файлах может быть что-то лишнее, но вроде диаграмму только смотрим
- по запросам и так вроде понятно, примеры не стал прикладывать